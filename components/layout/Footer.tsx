export function Footer() {
  return (
    <footer
      dir="rtl"
      style={{
        padding: "8px 16px",
        fontSize: "var(--text-overline)",
        fontWeight: 600,
        letterSpacing: "0.05em",
        color: "var(--foreground-faint)",
        borderTop: "1px solid var(--border-subtle)",
        background: "var(--surface)",
        textAlign: "center",
      }}
    >
      <p>
        מארקו — כלי מארקדאון חינמי בעברית
      </p>
    </footer>
  );
}
